export const metadata = {
  title: 'Video to Text — Extract Text from Video (Free OCR) | ConvertDox',
  description: 'Extract text from any video free. Capture frames manually or automatically and read the text with OCR. Your video stays in your browser — nothing is uploaded.',
  alternates: { canonical: 'https://convertdox.com/video-to-text' },
}
export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
