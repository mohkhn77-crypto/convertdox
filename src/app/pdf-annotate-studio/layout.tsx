export const metadata = {
  title: 'PDF Annotator — Draw & Mark Up PDFs Free | ConvertDox',
  description: 'Draw, mark up, and annotate PDF files free in your browser. Your file never leaves your device.',
  alternates: { canonical: 'https://convertdox.com/pdf-annotate-studio' },
}
export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
